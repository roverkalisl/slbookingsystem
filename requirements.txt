# ============================================
# SL Booking - Production Requirements ONLY
# For Render and production deployments
# ============================================

# Django Framework & REST API
# NOTE: djangorestframework must be >=3.16 for Django 6.x compatibility -
# 3.14.0 imports `cc_delim_re` from django.utils.cache, which Django 6
# removed, causing ImportError on every request (URLconf import failure).
Django==6.0.0
djangorestframework==3.18.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.3.1
django-filter==24.1
drf-spectacular==0.27.1

# Database
psycopg2-binary==2.9.10

# Static Files & Media
whitenoise==6.7.0
Pillow==11.3.0
cloudinary==1.41.0
django-cloudinary-storage==0.3.0

# Configuration
python-decouple==3.8
dj-database-url==2.1.0

# Web Server
gunicorn==23.0.0

# Async Tasks & Caching
celery==5.4.0
redis==5.1.0
django-redis==5.4.0

# Monitoring
sentry-sdk==2.0.0

# HTTP Requests
requests==2.32.3

# For development, use: pip install -r requirements-dev.txt
