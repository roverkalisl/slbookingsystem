# ============================================
# SL Booking - Production Requirements
# Python 3.12 - VERIFIED VERSIONS ONLY
# ============================================

# Django Framework & REST API
Django==5.0.1
djangorestframework==3.14.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.3.1
django-filter==24.1
drf-spectacular==0.27.1

# Database
psycopg2-binary==2.9.10

# Static Files & Media
whitenoise==6.7.0
Pillow==11.3.0

# Configuration
python-decouple==3.8
dj-database-url==2.1.0

# Web Server
gunicorn==23.0.0

# Async Tasks (Optional - if Celery is used)
celery==5.4.0
redis==5.1.0

# Monitoring (Optional - if Sentry is used)
sentry-sdk==2.0.0

# HTTP Requests
requests==2.32.3

# ============================================
# REMOVED - Not in current use:
# ============================================
# cloudinary==1.46.2  (OPTIONAL - if using Cloudinary)
# stripe==12.0.0      (OPTIONAL - if using Stripe payments)
# twilio==9.2.4       (OPTIONAL - if using Twilio SMS)
# django-sendgrid-v5  (NOT USED - use Django's email backend instead)
#
# NOTE: Email is handled via Django's default email backend
# Configure SMTP or SendGrid API via environment variables:
# - EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# - EMAIL_HOST=smtp.sendgrid.net
# - EMAIL_PORT=587
# - EMAIL_HOST_USER=apikey
# - EMAIL_HOST_PASSWORD=<sendgrid-api-key>
# ============================================

# Testing (Development only)
pytest==8.1.1
pytest-django==4.7.0
factory-boy==3.3.0
